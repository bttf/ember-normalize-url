import { normalizeUrl } from 'dummy/helpers/normalize-url';
import { module, test } from 'qunit';

module('Unit | Helper | normalize url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = normalizeUrl([42]);
  assert.ok(result);
});
